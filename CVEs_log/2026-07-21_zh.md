总结果数：271，高严重性：89，时间戳：2026-07-21 08:10:14
开始日期：现在；结束日期：2026-07-20

| 序号 | CVE ID | 基础评分 | 严重程度 | 描述 | 参考资料 |
|-----|--------|------------|----------|-------------|------------|
| 1 | CVE-2026-42566 | 7.5  | HIGH | Meshtastic 是一个开源的网格网络解决方案。在 2.7.23.b246bcd 版本之前，单个节点广播包含畸形字符编码的 User.long_name 可以通过 iOS 应用程序管理时使其他无线电设备无法通过 BLE 使用。畸形名称不需要恶意构造——它可能源于普通的缓冲区截断，并且已经在野外自然发生。至少有一条代码路径可以在多字节序列中间放置一个空终止符，从而在节点数据库中留下畸形的 User.long_name。问题在下游浮现：iOS 应用程序强制执行编码验证，因此一旦节点数据库中包含中毒条目就无法解析。这导致 BLE 同步进入失败/重试循环，从而导致对受影响设备失去控制。对于使用 iOS 应用程序管理其无线电设备的典型用户，设备实际上变得不可用，直到中毒节点从设备数据库中老化消失，或者除非他们有替代的管理路径（例如，Python CLI，可以用来手动识别和删除有问题的条目）。由于畸形名称会在网格中传播，单个受影响节点的临时存在可能会在很长一段时间内降低广大地理区域内的 iOS 用户的 BLE 管理体验。技术水平较低的用户没有直接的恢复途径。从 2.7.23.b246bcd 版本开始，固件已添加了输入清理和回归测试，以演示对已中毒设备的恢复。应用程序也采取了措施，以确保更优雅地处理畸形编码序列。 | [1]https://github.com/meshtastic/firmware/commit/2cc13a1132d94b66a9505e7f07ee2d3e83bd0c95<br>[2]https://github.com/meshtastic/firmware/security/advisories/GHSA-7ph5-2mjv-69h8 |
| 2 | CVE-2026-44359 | 10.0  | CRITICAL | Meshtastic 是一个开源的网格网络解决方案。在 2.7.21.1370b23 版本之前，Meshtastic 的 GitHub 仓库中的 main_matrix.yml 工作流由 pull_request_target 触发，并且多个任务会检出攻击者的 fork 代码并以访问仓库密钥和提升的 GITHUB_TOKEN 权限执行它。没有审批环节。来自外部用户的带有 author_association: "NONE" 的拉取请求会自动触发 CI 工作流。工作流直接从 fork 检出中执行攻击者控制的文件。这个问题可能导致供应链受损、自托管运行器受损和/或仓库接管。此问题与 GHSA-6mwm-v2vv-pp96 不同，后者解决了通过 github.head_ref 在 setup 任务中的命令注入问题。那个修复正确地将相关配置移到了环境变量中。然而，check、build 和 build-debian-src 任务中存在的更为严重的 fork 检出漏洞并未得到解决。版本 2.7.21.1370b23 包含了对此问题的补丁。 | [1]https://drive.google.com/file/d/1GdHT2s5hMYCiHt4zrWt1q58mvL7WQC0M/view?usp=sharing<br>[2]https://github.com/meshtastic/firmware/commit/5716aeba3bc1e1d34fba9567ff88917ede4a78a5<br>[3]https://github.com/meshtastic/firmware/security/advisories/GHSA-6mwm-v2vv-pp96<br>[4]https://github.com/meshtastic/firmware/security/advisories/GHSA-mjx5-98jq-q736 |
| 3 | CVE-2026-10081 | 8.8  | HIGH | Elementor的WordPress插件Unlimited Elements在2.0.11版本之前，没有对从Serp API获取的Google评论内容进行净化或转义就直接渲染到Google Reviews小部件输出中，这使得未经认证的攻击者可以通过在目标企业的Google列表上提交恶意评论，向任何显示该地点ID评论的WP页面访问者（包括管理员）传递存储型XSS攻击。 | [1]https://wpscan.com/vulnerability/e5cde405-5b34-4d3d-bfda-325402b0a5bc/ |
| 4 | CVE-2026-11349 | 8.6  | HIGH | 现代事件日历专业版WordPress插件在7.34.0之前，以及现代事件日历精简版WordPress插件在7.34.0之前，没有对请求参数进行净化和转义就直接在SQL语句中使用了它，这通过一个可供未认证用户访问的AJAX操作，导致了一个未认证的SQL注入漏洞，使攻击者能够从数据库中提取敏感数据。 | [1]https://wpscan.com/vulnerability/572229cb-8a09-406d-8623-7d6b553bfdde/ |
| 5 | CVE-2026-12592 | 7.5  | HIGH | SlimStat Analytics WordPress 插件在 5.5.0 版本之前未对访客控制的地理位置值进行转义，就将其输出到其管理分析报告中，这允许未经身份验证的访客存储一个跨站脚本负载，该负载会在查看报告的管理员的浏览器中执行。利用此漏洞需要 SlimStat Analytics WordPress 插件配置为使用 Cloudflare 地理位置提供商。 | [1]https://wpscan.com/vulnerability/3658cae6-f6ae-4ecc-8d74-bb402e61f5d5/ |
| 6 | CVE-2026-12970 | 7.1  | HIGH | LearnPress WordPress插件在4.4.1版本之前没有对搜索参数进行转义就将其反射到HTML属性中，导致了反射型跨站脚本攻击，该攻击会在被诱骗打开特制链接的已登录讲师或管理员的浏览器中执行。 | [1]https://wpscan.com/vulnerability/d0a2780f-ab13-4bb8-935d-2aeba1de12d2/ |
| 7 | CVE-2026-13147 | 9.1  | CRITICAL | Kirki WordPress 插件在 6.0.12 版本之前未对用户提供的 URL 进行验证就从服务器端请求该 URL，这使得未经身份验证的攻击者可以让网站向任意主机发送 HTTP 请求（服务器端请求伪造）。 | [1]https://wpscan.com/vulnerability/f39c3e35-8673-4095-a829-a9ec2624f66c/ |
| 8 | CVE-2026-16235 | 9.8  | CRITICAL | Crypt::Password 在 Perl 中的 0.28 及之前版本生成的随机盐值不安全。这些版本使用了内置的 rand 函数，该函数可预测且不适合用于加密。 | [1]https://metacpan.org/release/DRSTEVE/Crypt-Password-0.28/source/lib/Crypt/Password.pm#L306-309<br>[2]http://www.openwall.com/lists/oss-security/2026/07/20/3 |
| 9 | CVE-2026-6656 | 7.5  | HIGH | Crypt::Password 在 Perl 中的 0.28 及以下版本容易受到时间攻击。check_password 方法使用了内置的 eq 操作符。这使得可以通过时间差异来猜测底层的哈希值。 | [1]https://metacpan.org/release/DRSTEVE/Crypt-Password-0.28/source/lib/Crypt/Password.pm#L190-193<br>[2]https://rt.cpan.org/Ticket/Display.html?id=180162<br>[3]http://www.openwall.com/lists/oss-security/2026/07/20/4 |
| 10 | CVE-2026-9833 | 7.1  | HIGH | 标签组是“以高级方式显示您的分类术语”WordPress插件，在2.2.0版本之前，该插件没有正确转义其AJAX参数之一，就将其反映在以HTML内容类型提供的响应正文中。这使得未经身份验证的攻击者可以通过诱使具有`edit_pages`权限（编辑或更高权限）的已登录用户点击精心构造的链接，在该用户的浏览器中执行任意JavaScript代码。 | [1]https://wpscan.com/vulnerability/75792c33-2d9d-46db-a98c-00ec40951099/ |
| 11 | CVE-2026-13577 | 8.2  | HIGH | Dancer2 版本直到 2.1.0 在 Perl 中生成不安全的会话 ID，当 CSPRNG 模块不可用时。当 Math::Random::ISAAC::XS 和 Crypt::URandom 都不可用时，Dancer2::Core::Role::SessionFactory::generate_id 会静默地回退到使用内置的基于 rand 的会话 ID。回退的会话 ID 是从对内置 rand 函数的一次调用结果、Dancer2::Core::Role::SessionFactory 模块的绝对路径、一个内部计数器、进程 ID、模块实例的内存地址以及一个打乱顺序的字符串（使用 List::Util::shuffle 函数，该函数也使用了内置的 rand 函数）进行 SHA-1 哈希生成的。这些都是低熵且容易被猜测的数据源。内置的 rand() 函数以 32 位种子初始化，并被认为不适合用于安全应用。可预测的会话 ID 可能允许攻击者获取系统访问权限。 | [1]https://github.com/PerlDancer/Dancer2/blob/v2.1.0/lib/Dancer2/Core/Role/SessionFactory.pm#L142<br>[2]https://www.cve.org/CVERecord?id=CVE-2026-5080 |
| 12 | CVE-2026-16242 | 9.4  | CRITICAL | 在托管控制平面的Konnectivity代理服务器配置中发现了一个缺陷。面向代理的监听器在没有使用--cluster-ca-cert（并且没有基于令牌的代理身份验证）的情况下启动，因此客户端证书未经过验证。远程攻击者如果能够访问到Konnectivity集群端点，就可以作为未经身份验证的代理连接，加入路由池，并有可能代理、检查、修改或丢弃从控制平面到节点的流量。 | [1]https://access.redhat.com/security/cve/CVE-2026-16242<br>[2]https://bugzilla.redhat.com/show_bug.cgi?id=2502690<br>[3]https://github.com/openshift/hypershift/pull/9031 |
| 13 | CVE-2026-14448 | 7.2  | HIGH | 一个高权限的远程攻击者可以利用系统证书视图中存在的经过身份验证的操作系统命令注入漏洞，这是由于在操作系统命令中未正确处理特殊元素。这可能导致机密性、可用性和完整性的完全丧失。 | [1]https://www.certvde.com/en/advisories/VDE-2026-044/<br>[2]https://www.certvde.com/en/advisories/VDE-2026-058/ |
| 14 | CVE-2026-16246 | 7.3  | HIGH | 在BRAIN2 3.09之前的版本中，应用程序LogPathConfig.exe在安装过程中会被执行。因此，Windows组“Everyone”被授予对%ProgramData%的完全控制权，而不是仅限于%ProgramData%\Bizerba\BRAIN2\。从BRAIN2 3.09开始，安装过程不再执行此工具。然而，可选组件Bizerba ScriptService仍然会执行它。Bizerba ScriptService正在被弃用，并且从BRAIN2版本3.11开始将不再包含该组件。 | [1]https://www.bizerba.com/downloads/global/information-security/2026/bizerba-sa-2026-0003.pdf |
| 15 | CVE-2026-16247 | 7.3  | HIGH | 在_connect.BRAIN 5.06之前的版本中，应用程序LogPathConfig.exe会在安装过程中执行。在此过程中，%ProgramData%上现有的权限会被删除并替换，授予Windows组“Everyone”完全控制权，而不是限制对%ProgramData%\Bizerba\_connect.BRAIN或%ProgramData%\Bizerba\BCT的访问。从_connect.BRAIN 5.06开始，安装过程不再执行此工具。 | [1]https://www.bizerba.com/downloads/global/information-security/2026/bizerba-sa-2026-0003.pdf |
| 16 | CVE-2026-63735 | 8.1  | HIGH | SurrealDB 3.2.0之前的版本未能在自定义API路由中验证命名空间和数据库范围，导致已认证用户可以调用不同命名空间/数据库中的端点。拥有任何命名空间/数据库的有效凭据的攻击者可以通过在URL路径中指定目标范围来访问其他租户中的自定义API端点，从而读取敏感数据或触发非预期操作。 | [1]https://github.com/surrealdb/surrealdb/security/advisories/GHSA-848m-r628-vrxw<br>[2]https://www.vulncheck.com/advisories/surrealdb-before-authentication-bypass-via-custom-api |
| 17 | CVE-2026-63739 | 7.7  | HIGH | SurrealDB 在 3.1.5 版本之前存在一个任意文件读取漏洞，该漏洞位于 DEFINE ANALYZER 映射器过滤器中，允许具有 EDITOR 或 OWNER 角色的数据库用户读取 SurrealDB 进程可访问的文件。当 SURREAL_FILE_ALLOWLIST 为空或未配置时，攻击者可以在映射器过滤器中指定任意文件路径，并通过查询错误信息来获取文件内容。 | [1]https://github.com/surrealdb/surrealdb/security/advisories/GHSA-cc8f-fcx3-gpjr<br>[2]https://www.vulncheck.com/advisories/surrealdb-before-arbitrary-file-read-via-define-analyzer |
| 18 | CVE-2026-63747 | 7.5  | HIGH | SurrealDB 3.1.0之前的版本在RPC使用处理器中存在一个拒绝服务漏洞，当在没有命名空间的情况下设置db时会引发恐慌。未经身份验证的攻击者可以向/rpc端点发送格式错误的WebSocket消息以使服务器进程崩溃。 | [1]https://github.com/surrealdb/surrealdb/security/advisories/GHSA-wjjj-24cx-f28g<br>[2]https://www.vulncheck.com/advisories/surrealdb-before-denial-of-service-via-malformed-rpc-use |
| 19 | CVE-2026-63756 | 8.1  | HIGH | SurrealDB 3.1.0之前的版本在HTTP /rpc端点中存在一个检查时间/使用时间的竞态条件，这使得未经认证的请求可以继承已认证会话的状态。未经认证的攻击者可以在合法的已认证流量活跃时向/rpc端点发送并发请求，以劫持用户权限执行操作。 | [1]https://github.com/surrealdb/surrealdb/security/advisories/GHSA-4vgr-h27g-cf9p<br>[2]https://www.vulncheck.com/advisories/surrealdb-before-privilege-escalation-via-rpc-session-race-condition |
| 20 | CVE-2026-63757 | 8.8  | HIGH | SurrealDB 3.1.0 之前的版本存在一个会话劫持漏洞，其中 HTTP /rpc sessions 方法在未经身份验证的情况下返回附加的会话 UUID，并且接受任意会话字段而没有所有权验证。未经身份验证的攻击者可以枚举会话 UUID 并冒充已认证的会话来读取、写入、删除数据以及提升权限。 | [1]https://github.com/surrealdb/surrealdb/security/advisories/GHSA-5qfp-32cf-69jh<br>[2]https://www.vulncheck.com/advisories/surrealdb-before-session-hijacking-via-rpc-sessions |
| 21 | CVE-2026-63760 | 7.5  | HIGH | SurrealDB 在 3.1.0 之前的版本在处理嵌套的大括号、中括号或圆括号时，未能强制执行配置的递归深度限制。未经身份验证的攻击者可以向 WebSocket /rpc 端点发送深度嵌套的 JSON 负载，以耗尽服务器内存并导致进程崩溃。 | [1]https://github.com/surrealdb/surrealdb/security/advisories/GHSA-q729-696q-g9pq<br>[2]https://www.vulncheck.com/advisories/surrealdb-before-denial-of-service-via-json-parser |
| 22 | CVE-2026-64620 | 9.8  | CRITICAL | FreeRDP 在 3.28.0 之前（受影响版本 <=3.27.1）包含一个在 crypto_rsa_common() 函数中的基于堆的缓冲区溢出（位于 libfreerdp/crypto/crypto.c 文件中）。该函数通过 BN_bn2bin() 将模幂结果写入调用者的输出缓冲区，然后才检查 output_length > out_length，因此在边界检查之前就已经写入了越界的字节。在服务器端，当客户端选择 RDP 标准安全时，加密的客户端随机数会被解密到一个固定 32 字节的缓冲区中。由于服务器发布了其 RSA 公钥，未经身份验证的攻击者可以伪造一个解密值达到完整模长的密文（例如，对于 RSA-2048 为 256 字节），从而在认证前溢出 32 字节的堆缓冲区，导致多达约 224 字节的受攻击者控制的数据溢出，最终造成拒绝服务。 | [1]https://github.com/FreeRDP/FreeRDP/commit/1f7a716d39b5605bb8a83b0c3c97a6ce386609ef<br>[2]https://github.com/FreeRDP/FreeRDP/security/advisories/GHSA-pjqx-v446-x7fc<br>[3]https://www.vulncheck.com/advisories/freerdp-before-heap-buffer-overflow-via-crypto-rsa-common |
| 23 | CVE-2026-64621 | 7.3  | HIGH | FreeRDP 在 3.28.0 之前（受影响的版本为 3.x 到 3.27.1）在解析 .rdp 连接文件中的 selectedmonitors 字段时，存在一个双重释放漏洞 (double-free vulnerability) 在 freerdp_client_rdp_file_apply_to_settings() 函数中 (client/common/file.c)。MonitorIds 数组是通过设置对象分配的，而在 strtoul 错误路径上，一个原始非拥有指针会被释放，而不会清除 settings->MonitorIds，导致其悬空；在拆解时，freerdp_settings_free() 会再次释放相同的缓冲区。攻击者可以通过诱使受害者打开一个带有超大监视器令牌的特制 .rdp 文件，在默认配置下触发任何 FreeRDP CLI 客户端（如 xfreerdp/sdl-freerdp/wlfreerdp）中的大小可控的双重释放漏洞。 | [1]https://github.com/FreeRDP/FreeRDP/commit/1f7a716d39b5605bb8a83b0c3c97a6ce386609ef<br>[2]https://github.com/FreeRDP/FreeRDP/security/advisories/GHSA-f27x-frr8-j9hc<br>[3]https://www.vulncheck.com/advisories/freerdp-before-double-free-via-selectedmonitors |
| 24 | CVE-2026-64622 | 7.5  | HIGH | Network-AI (npm: network-ai) 版本 5.12.2 到 5.13.3 未能对 ApprovalInbox GET 读取路由应用配置的授权检查（checkAuth/secret），因此即使操作员配置了密钥，未经身份验证的用户也可以访问敏感的审批请求详情。GET /approvals/?status=all、GET /approvals/:id、GET /approvals/stats 和 GET /approvals/sse 路由会披露完整的 ApprovalEntry 内容，包括 action/target shell 命令字符串、文件路径、理由和风险级别。所有响应还携带一个硬编码的 Access-Control-Allow-Origin: * 头，使得从操作员访问的任何网站都可以进行跨源披露。这是对 GHSA-mxjx-28vx-xjjj 的不完全修复。 | [1]https://github.com/Jovancoding/Network-AI/security/advisories/GHSA-m4jg-6w3q-gm86<br>[2]https://www.vulncheck.com/advisories/network-ai-through-missing-authorization-via-approvalinbox<br>[3]https://github.com/Jovancoding/Network-AI/security/advisories/GHSA-m4jg-6w3q-gm86 |
| 25 | CVE-2026-64623 | 8.6  | HIGH | 5.13.4之前的Network-AI版本在APSAdapter中存在不正确的加密签名验证漏洞，其中默认的本地验证器接受任何非空字符串作为有效。未经身份验证的攻击者可以提交带有任意范围的伪造APS委托负载，以绕过签名验证，并获取针对敏感资源（包括SHELL_EXEC）的已签名权限授予令牌。 | [1]https://github.com/Jovancoding/Network-AI/security/advisories/GHSA-3jf7-33vc-hgf4<br>[2]https://www.vulncheck.com/advisories/network-ai-before-cryptographic-signature-verification-bypass<br>[3]https://github.com/Jovancoding/Network-AI/security/advisories/GHSA-3jf7-33vc-hgf4 |
| 26 | CVE-2026-12080 | 7.3  | HIGH | 在QEMU Guest Agent (qga)中发现了一个漏洞。本地无特权用户可以通过操纵符号链接来利用guest-ssh-add-authorized-keys命令处理程序中的漏洞。这种情况可能通过确定性的目录符号链接绕过或时间检查到使用（TOCTOU）文件符号链接竞争发生。成功利用该漏洞可以让攻击者获得任意root拥有的文件或目录的所有权，从而导致获取root访问权限。此漏洞需要一个外部管理层（例如，libvirt）来触发受影响的代码路径。 | [1]https://access.redhat.com/security/cve/CVE-2026-12080<br>[2]https://bugzilla.redhat.com/show_bug.cgi?id=2499603<br>[3]https://gitlab.com/qemu-project/qemu/-/work_items/3929 |
| 27 | CVE-2026-16248 | 8.8  | HIGH | 在Tenda AC10 16.03.10.09_multi_TDE01中发现了一个漏洞。此问题影响到组件httpd/netctrl中的/goform/AdvSetLanip文件的fromAdvSetLanip函数。通过操纵参数GetValue/SetValue，可导致基于栈的缓冲区溢出。攻击可以从远程执行。该漏洞利用已被公开，并可能被使用。 | [1]https://github.com/teiwiet/tenda-ac10-vulnerabilities/blob/main/advisory-fromAdvSetLanip.md<br>[2]https://vuldb.com/cve/CVE-2026-16248<br>[3]https://vuldb.com/submit/858411<br>[4]https://vuldb.com/vuln/380539<br>[5]https://vuldb.com/vuln/380539/cti<br>[6]https://www.tenda.com.cn/ |
| 28 | CVE-2026-12701 | 9.0  | CRITICAL | 在pulpcore中发现了一个路径遍历漏洞。relative_path_validator函数仅验证内容路径是否不以"/"开头，但未能阻止路径中任何位置的目录遍历序列（如"../"）。经过身份验证的管理员可以构造一个包含嵌入式遍历序列的relative_path（例如："looking/normal/../../../../etc/shadow"），这会在FilesystemExport操作期间逃出预期的导出目录。由于文件内容也是用户可控的（上传的工件），这允许将任意文件写入Pulp服务用户可写的任何位置，可能会导致服务被破坏或进一步的系统利用。 | [1]https://access.redhat.com/errata/RHSA-2026:42078<br>[2]https://access.redhat.com/errata/RHSA-2026:42079<br>[3]https://access.redhat.com/errata/RHSA-2026:42082<br>[4]https://access.redhat.com/errata/RHSA-2026:42150<br>[5]https://access.redhat.com/errata/RHSA-2026:42151<br>[6]https://access.redhat.com/errata/RHSA-2026:42240<br>[7]https://access.redhat.com/security/cve/CVE-2026-12701<br>[8]https://bugzilla.redhat.com/show_bug.cgi?id=2490703 |
| 29 | CVE-2026-16252 | 7.3  | HIGH | 在北京神州视翰科技多媒体综合业务显示系统8.2.2中发现了一个安全漏洞。受影响的是文件/admin/system/structure/updateStructure/deflate/Insecure/Staffshinel Ds.jsp?Shine ID=aaa中的一个未知功能。对参数Structure_ID的操作会导致SQL注入。可以远程发起攻击。该漏洞的利用方法已被公开，可能被用于攻击。 | [1]https://ucn9h68n9289.feishu.cn/docx/XSuvdAP8foTOKzxEnZaclsZznMb?from=from_copylink<br>[2]https://vuldb.com/cve/CVE-2026-16252<br>[3]https://vuldb.com/submit/858453<br>[4]https://vuldb.com/vuln/380551<br>[5]https://vuldb.com/vuln/380551/cti<br>[6]https://github.com/gtsteffaniak/filebrowser/security/advisories/GHSA-vvp7-h4fj-m28w |
| 30 | CVE-2026-45270 | 8.7  | HIGH | CI4MS 是一个基于 CodeIgniter 4 的内容管理系统框架。在 0.31.9.0 版本之前，`Pages` 后端模块对语言键控页面内容注册了 `html_purify` 验证规则，但将原始的、未经净化的 POST 值保存到数据库中。页面的公共渲染器（`Home::index()` → `app/Views/templates/default/pages.php`）在输出 `$pageInfo->content` 时没有使用 `esc()`，从而导致存储型 XSS 漏洞，该漏洞会在每个访问受影响页面的用户（包括管理员）触发。由于页面可能被提升为站点首页，因此有效载荷可以在 `/` 路径下提供，并影响到站点的每一个访客。0.31.9.0 版本修复了这个问题。 | [1]https://github.com/ci4-cms-erp/ci4ms/releases/tag/0.31.9.0<br>[2]https://github.com/ci4-cms-erp/ci4ms/security/advisories/GHSA-gqr2-7hcg-rchf<br>[3]https://github.com/ci4-cms-erp/ci4ms/security/advisories/GHSA-gqr2-7hcg-rchf |
| 31 | CVE-2026-54910 | 7.7  | HIGH | FileBrowser Quantum 是一个免费的、自托管的、基于 Web 的文件管理器。在 1.4.3-beta 版本之前，`subtitlesHandler` 端点 (`GET /api/media/subtitles`) 接受两个用户控制的查询参数：`path` 和 `name`，这两个参数在没有进行任何清理的情况下被用于文件系统操作，从而创建了两个独立的路径遍历向量。主要的向量是 `path` 参数：它直接传递给 `idx.GetRealPath()` 而没有调用 `SanitizeUserPath()`，允许攻击者逃离存储根目录并将 `parentDir` 设置为主机上的任意目录。不需要现有的锚文件。次要的向量是 `name` 参数：它通过 `filepath.Join(parentDir, name)` 与 `parentDir` 连接，而没有剥离目录组件，允许相对于任何解析的 `parentDir` 进行遍历。任何经过身份验证的用户（无论其角色或权限如何）都可以利用任一向量来读取服务器进程可读的任何文本文件，包括 `/etc/passwd`、SSH 密钥、数据库凭据和 JWT 签名密钥。1.4.3-beta 版本修复了这个问题。 | [1]https://github.com/gtsteffaniak/filebrowser/commit/f3f4bbe80cb569d664174aea874d7bfa008c3b5a<br>[2]https://github.com/gtsteffaniak/filebrowser/security/advisories/GHSA-vvp7-h4fj-m28w<br>[3]https://github.com/gtsteffaniak/filebrowser/security/advisories/GHSA-vvp7-h4fj-m28w |
| 32 | CVE-2026-63090 | 8.8  | HIGH | ProFTPD 在 1.3.9c 和 1.3.10rc3 之前的版本中，mod_sftp 模块存在一个基于堆的缓冲区溢出漏洞，该漏洞允许经过身份验证的低权限攻击者通过发送精心构造的 SFTP 数据包片段（超过 fxp.c 组件中的 16 KB 重组缓冲区）来实现任意代码执行。攻击者可以提供过大的片段以触发错误条件下的重新分配，破坏池空闲列表元数据，覆盖 root_fs BSS 全局指针以引用伪造的文件系统结构，并通过精心构造的 RENAME 请求将 pr_fsio_stat() 重定向到 system()。 | [1]https://github.com/proftpd/proftpd/blob/master/RELEASE_NOTES<br>[2]https://github.com/proftpd/proftpd/commit/4ee8701bcf425f11b3b2116e634ff3e655d918b1<br>[3]https://github.com/proftpd/proftpd/issues/2190<br>[4]https://github.com/proftpd/proftpd/releases/tag/v1.3.10rc3-3<br>[5]https://github.com/proftpd/proftpd/releases/tag/v1.3.9c<br>[6]https://www.vulncheck.com/advisories/proftpd-mod-sftp-heap-buffer-overflow-via-sftp-packet-reassembly |
| 33 | CVE-2026-21824 | 8.8  | HIGH | HCL Commerce 存在一个权限提升漏洞，该漏洞可能导致拒绝服务、用户个人数据泄露以及执行未经授权的管理操作。 | [1]https://support.hcl-software.com/csm?id=kb_article&sysparm_article=KB0130114 |
| 34 | CVE-2026-25039 | 8.8  | HIGH | Parsec 是一个基于云的应用程序，用于简单且加密安全的文件共享。该应用程序不对工作区名称进行清理，如果该工作区名称是UNC路径，则会创建一个漏洞。在Windows文件系统中创建挂载点以挂载组织的工作区时，应用程序不会清理工作区名称。如果工作区名称被评估为UNC路径（因为允许名称中包含`\`字符），则会导致问题。如果UNC路径无效（或目标资源不可用），应用程序将变得无响应；否则，系统将与挂载的UNC路径交互，使攻击者能够检索到[`NTLM`]哈希。 | [1]https://github.com/Scille/parsec-cloud/security/advisories/GHSA-qx56-wxpm-j4m6<br>[2]https://github.com/Scille/parsec-cloud/security/advisories/GHSA-qx56-wxpm-j4m6 |
| 35 | CVE-2026-28220 | 8.4  | HIGH | Wazuh 是一个免费且开源的平台，用于威胁预防、检测和响应。在 4.14.5 版本之前，集群分布式 API（DAPI）处理中存在一些问题，这些问题允许集群对等节点或任何能够使用共享集群密钥进行身份验证的参与者，使主节点反序列化攻击者控制的可调用对象，并在其控制的 RBAC 上下文中执行。`framework/wazuh/core/cluster/common.py` 中的集群代码使用 `as_wazuh_object()` 反序列化 JSON，该方法解析任何顶级包为 wazuh 或 api 的可调用对象（这是一个由 `ALLOWED_CALLABLES_PACKAGES` 控制的过于宽泛的允许列表），并且在 `framework/wazuh/core/cluster/dapi/dapi.py` 中处理的 DAPI 请求接受客户端提供的 `rbac_permissions` 值，`run_local()` 将其应用为全局 RBAC 上下文，因此提供一个 `rbac_mode` 为 black 的值会导致对 `expose_resources` 保护函数的授权检查通过，而无需任何合法的权限分配。这些漏洞结合在一起，允许在主节点上执行特权管理操作，例如在 WAZUH_PATH 下进行任意文件写入、创建新的 API 用户以及篡改 security.yaml，并可以链式操作导致完全接管管理器。此问题已在 4.14.5 版本中修复。 | [1]https://github.com/wazuh/wazuh/security/advisories/GHSA-w2jj-pfq9-mh9p<br>[2]https://github.com/wazuh/wazuh/security/advisories/GHSA-w2jj-pfq9-mh9p |
| 36 | CVE-2026-32807 | 7.5  | HIGH | dataCycle 是一个数据管理系统，用于集中存储、管理、搜索、查找和分发数据。在 dataCycle-CORE 中，该模块负责核心处理和框架规则，在版本 25.07.3 及之前，任何拥有 DataLink UUID 的人都可以直接获取所附的文本文件，即使链接已过期、调用者未经身份验证或正常的显示流程会拒绝访问也是如此。由于路由是公开的，并且邮件发送程序嵌入了直接文件 URL，因此任何泄露、转发、记录或过时的电子邮件链接都可以继续暴露附件。 | [1]https://github.com/datacycle-engine/dataCycle-CORE/security/advisories/GHSA-gj99-3h7x-v4rv |
| 37 | CVE-2026-35198 | 9.0  | CRITICAL | HeyForm 是一个开源表单构建器。在 3.0.0-rc.7 版本之前，该表单构建器中存在一个存储型跨站脚本 (XSS) 漏洞，允许低权限团队成员注入恶意 JavaScript，当团队所有者查看表单时，这些脚本会被执行，从而通过权限提升导致完全账户接管。3.0.0-rc.7 版本包含对该问题的修复补丁。 | [1]https://github.com/heyform/heyform/commit/cc97d27a57ae400fec23abf5dcf6f9533c3b5db3<br>[2]https://github.com/heyform/heyform/security/advisories/GHSA-chmm-jqpm-3pwx<br>[3]https://github.com/heyform/heyform/security/advisories/GHSA-chmm-jqpm-3pwx |
| 38 | CVE-2026-45713 | 7.5  | HIGH | Mailpit 是一个面向开发者的电子邮件测试工具和 API。在 1.30.0 版本之前，Mailpit 的 SMTP 服务器有一个 Server.MaxSize 整型字段，用于控制允许的最大 DATA 负载大小，但该字段除了在测试代码中之外从未被赋值，因此它保持为 Go 的零值（0 ⇒ “无限制”）。同样的情况也适用于 HTTP /api/v1/send 端点，其请求体通过 json.NewDecoder(r.Body) 解码，并且没有使用 http.MaxBytesReader。由于 Mailpit 默认监听器绑定到 [::]:1025 (SMTP) 和 [::]:8025 (HTTP)，并且两者都不需要身份验证，因此单个网络可达的攻击者可以向 Mailpit 发送任意大的消息，并观察内存消耗激增，放大倍数约为 7-10 倍（原始帧 → enmime 封装树 → 搜索文本索引 → zstd 编码写入 SQLite）。重复攻击或从多个连接同时进行攻击会导致进程因内存不足而被杀死。1.30.0 版本包含了一个修复补丁。 | [1]https://github.com/axllent/mailpit/releases/tag/v1.30.0<br>[2]https://github.com/axllent/mailpit/security/advisories/GHSA-fpxj-m5q8-fphw<br>[3]https://github.com/axllent/mailpit/security/advisories/GHSA-fpxj-m5q8-fphw |
| 39 | CVE-2026-46412 | 10.0  | CRITICAL | `@beproduct/nestjs-auth` 是一个支持 OpenID Connect 的 BeProduct IDS（身份服务器）的 NestJS 认证模块。在 2026-05-11 20:19 UTC 到 22:56 UTC 期间，攻击者使用了一个被泄露的 npm 发布令牌发布了 18 个恶意版本的 `@beproduct/nestjs-auth`（从 0.1.2 到 0.1.19）。安装后载荷试图窃取 npm 令牌（来自 `~/.npmrc`）；GitHub 个人访问令牌、OAuth 令牌（`gho_*`）和 Actions OIDC 令牌；AWS 凭据（来自环境变量和 `~/.aws/credentials`）；HashiCorp Vault 令牌；以及存在于环境变量中的其他秘密。版本 `0.1.20` 是从原始 `0.1.1` 源代码树重新发布的干净版本。任何安装了 `>=0.1.2 <=0.1.19` 范围内任一版本的人都应移除该包并清理 npm 缓存；安装干净版本；轮换安装环境中存在的所有凭据，包括所有 npm 发布令牌、所有 GitHub 个人访问令牌和 OAuth 令牌、AWS 访问密钥、HashiCorp Vault 令牌，以及安装时在环境变量或配置文件中的任何其他秘密；扫描受影响的主机以查找入侵迹象，如果发现任何迹象，则将主机视为已被入侵并重新镜像；检查提交的仓库历史记录中 `.claude/` 或 `.vscode/` 目录中是否有意外添加的内容。已知蠕虫会通过自动化代理运行时将 `setup.mjs` 及钩子配置提交到 PR 分支。 | [1]https://github.com/BeProduct/beproduct-org-nestjs-auth/security/advisories/GHSA-6xwp-cp5h-q856<br>[2]https://www.aikido.dev/blog/checklist-github-actions<br>[3]https://www.aikido.dev/blog/mini-shai-hulud-is-back-tanstack-compromised |
| 40 | CVE-2026-46415 | 8.2  | HIGH | Caddy Defender 插件是 Caddy 的一个中间件，允许用户根据客户端的 IP 地址来阻止或修改请求。在 0.10.1 版本之前，Caddy Defender 在判断是否应阻止请求时使用了 `r.RemoteAddr`。`RemoteAddr` 是直接连接到 Caddy 的对端地址。在 Caddy 部署于可信代理、CDN 或负载均衡器之后的情况下，直接对端通常是代理，而不是原始客户端。Caddy 在应用配置的 `trusted_proxies` 策略后会将原始客户端地址解析为其 `client_ip` 请求变量，但 Defender 并未使用该值。因此，来自被封禁 IP 范围的客户端可以通过其自身 IP 地址未被封禁的可信代理访问 Caddy 从而绕过 Defender。这影响了那些期望基于真实客户端 IP 实施封禁且位于可信代理后的 Defender 部署。此问题已在 0.10.1 版本中修复，通过使 Defender 在可用时优先使用 Caddy 解析出的 `client_ip` 请求变量。只有当 Caddy 没有提供解析后的客户端 IP 时，Defender 才会回退到使用 `RemoteAddr`。对于依赖 Caddy 可信代理客户端 IP 解析功能的受影响 Defender 版本，没有完全的解决办法。在升级前，受影响用户应在流量到达 Caddy 之前的可信代理、CDN、负载均衡器、防火墙或其他边缘层实施等效的 IP 封禁。如果 Caddy 直接从客户端接收流量，而没有中间的可信代理，则不受此绕过问题的影响。 | [1]https://caddyserver.com/docs/caddyfile/matchers#client-ip<br>[2]https://caddyserver.com/docs/caddyfile/options#trusted-proxies<br>[3]https://github.com/JasonLovesDoggo/caddy-defender/pull/139<br>[4]https://github.com/JasonLovesDoggo/caddy-defender/security/advisories/GHSA-3h23-rrpc-3p87 |
| 41 | CVE-2026-51027 | 9.9  | CRITICAL | FileThingie v.2.5.7 中的一个问题允许远程攻击者通过 ft2.php 组件获取敏感信息。 | [1]https://github.com/SpeWnz/Vulnerability-Research/tree/main/CVE-2026-51027<br>[2]https://github.com/leefish/filethingie<br>[3]https://github.com/SpeWnz/Vulnerability-Research/tree/main/CVE-2026-51027 |
| 42 | CVE-2026-63429 | 8.6  | HIGH | HeyForm 是一个开源表单构建器。在 3.0.0-rc.9 版本之前，`POST /api/upload` 没有身份验证防护、没有全局防护、没有表单上下文验证、不需要 `openToken`，也没有会话 cookie 检查。任何匿名互联网用户都可以上传文件（PDF、DOC/DOCX、XLS/XLSX、CSV、TXT、MP4、图片等，最大 10 MB），并在 HeyForm 域名上获得一个永久的公开 URL。该端点既被已认证的表单创建者使用，也被未认证的表单提交者使用；由于没有表单上下文绑定，所有对该端点的请求都被匿名接受。版本 3.0.0-rc.9 包含了针对此问题的修复补丁。 | [1]https://github.com/heyform/heyform/commit/092e255e9e02565de1b3c057f3dad849160952d2<br>[2]https://github.com/heyform/heyform/security/advisories/GHSA-432x-54v2-p7p7<br>[3]https://github.com/heyform/heyform/security/advisories/GHSA-432x-54v2-p7p7 |
| 43 | CVE-2026-32806 | 7.5  | HIGH | dataCycle 是一个数据管理系统，用于集中存储、管理、搜索、查找和分发数据。在 dataCycle-CORE 中，处理核心处理和框架规则的模块，在包括 25.07.3 版本及之前的所有版本中，任何经过身份验证的用户都可以通过 /remote_render 请求任意的部分或辅助支持的渲染函数。该端点不限制可以渲染哪些部分，并且在渲染选定视图之前不应用特定控制器的授权。这使得低权限用户能够检索到原本被导航和路由检查隐藏的服务器端渲染的管理员内容。在测试实例中，标准用户能够获取 PostgreSQL 管理员仪表板统计数据，尽管 /admin 本身会重定向。此问题已在 26.06.08 版本中修复。 | [1]https://github.com/datacycle-engine/dataCycle-CORE/security/advisories/GHSA-xc6g-2v4c-456c |
| 44 | CVE-2026-32820 | 7.5  | HIGH | dataCycle 是一个数据管理系统，用于集中存储、管理、搜索、查找和分发数据。在 dataCycle-CORE 中，处理核心处理和框架规则的模块，在版本 25.07.3 及之前，文档和静态 Markdown 渲染器接受攻击者控制的路径段，并仅通过 Rails HTML 清理器进行处理，而该清理器不会移除目录遍历序列。未经身份验证的攻击者可以遍历出预期的 `docs` 或 `static` 目录，并从应用程序根目录或引擎根目录渲染任意 `.md` 文件。此问题已在版本 26.06.08 中修复。 | [1]https://github.com/datacycle-engine/dataCycle-CORE/security/advisories/GHSA-gc37-6w59-hj36 |
| 45 | CVE-2026-32821 | 8.1  | HIGH | dataCycle是一个用于集中存储、管理、搜索、查找和分发数据的数据管理系统。在dataCycle-CORE中，该模块负责核心处理和框架规则，在25.07.3版本及之前，任何拥有自己访问令牌的认证API用户都可以通过提供`user_email`让集合API以另一个用户的身份评估权限。如果目标用户有集合，则这可能会通过API暴露这些集合。在V4中，一旦知道了集合ID，同一个控制器还提供了`add_item`和`remove_item`路由，而没有任何对象级别的`authorize!`检查，这就可能创建了一个跨用户的修改路径。此问题已在26.06.08版本中得到修复。 | [1]https://github.com/datacycle-engine/dataCycle-CORE/security/advisories/GHSA-vjjr-9q8g-mgx7 |
| 46 | CVE-2026-32824 | 7.3  | HIGH | dataCycle 是一个数据管理系统，用于集中存储、管理、搜索、查找和分发数据。在 dataCycle-CORE 中，该模块处理核心处理和框架规则，在版本 25.07.3 及之前，低权限的经过身份验证的 API 用户可以在触发密码重置或确认流程时提供 `forwardToUrl` 和 `redirectUrl` 值。这些值随后会被嵌入到外发电子邮件工作流中，而无需主机白名单。这创建了两个相关的滥用路径：<br>- 密码重置或确认链接可以发送给受害者，并且令牌已经附加到攻击者控制的 `forwardToUrl`<br>- 在合法的密码重置完成后，浏览器会被重定向到攻击者控制的 `redirectUrl`<br><br>实际上，这可以被用于网络钓鱼、令牌捕获、确认劫持，或将受害者从可信电子邮件引导至攻击者的域名。此漏洞已在版本 26.06.08 中修复。 | [1]https://github.com/datacycle-engine/dataCycle-CORE/security/advisories/GHSA-8jfx-wpjg-hf38 |
| 47 | CVE-2026-32825 | 7.3  | HIGH | dataCycle 是一个数据管理系统，用于集中存储、管理、搜索、查找和分发数据。在 dataCycle-CORE 中，处理核心处理和框架规则的模块，在包括版本 25.07.3 之前，应用程序接受对浏览器登录流程和 JSON 登录端点的无限制密码猜测。源代码在用户模型上启用了 Devise 的 `:lockable` 模块，但明确禁用了锁定和解锁策略，并且在 Rails 代码中没有发现请求节流或速率限制层。这直接造成了在线密码猜测风险：<br>- 有效用户账户可以连续受到攻击而不会被临时锁定<br>- 该弱点可以通过 `/users/sign_in` 和 `/api/v4/auth/login` 两个路径访问<br>- 成功猜测后，在 HTML 流程中会获得一个正常的会话 cookie，在 API 流程中会获得一个新的 JWT<br>- 由于不需要 CSRF 令牌，API 端点特别适合自动化攻击<br><br>此问题已在版本 26.06.08 中修复。 | [1]https://github.com/datacycle-engine/dataCycle-CORE/security/advisories/GHSA-736f-cqq3-ccgf |
| 48 | CVE-2026-35048 | 9.8  | CRITICAL | Piwigo 安装程序在 16.3.0 及更早版本中接受用于数据库配置的 POST 参数，并将它们直接写入 PHP 配置文件，而没有进行适当的清理。在 PHP 8+ 中，`addslashes()` 保护被绕过，因为它检查了 `get_magic_quotes_gpc()` 函数，该函数在 PHP 8.0 中已被移除。这允许原始用户输入直接插入到 PHP 源代码中。未经身份验证的攻击者可以通过 POST 参数（prefix, dbpasswd, dbhost, dbname 或 dbuser）注入任意 PHP 代码，这些代码会被写入 `local/config/database.inc.php` 并在每次页面加载时执行。 | [1]https://github.com/Piwigo/Piwigo/security/advisories/GHSA-gphq-34pv-gvf3<br>[2]https://github.com/Piwigo/Piwigo/security/advisories/GHSA-gphq-34pv-gvf3 |
| 49 | CVE-2026-39879 | 7.1  | HIGH | 由于在 [`afsql_dd_run_query`](https://github.com/syslog-ng/syslog-ng/blob/649e6e18e3459fb4467000a88dfb12fa97f9719c/modules/afsql/afsql.c#L219) 中缺少清理调用，syslog-ng 在 4.12 版本之前容易受到来自不受信任来源的 SQL 注入攻击。这并不是默认配置的一部分，SQL 驱动程序需要手动配置。修复措施已在 syslog-ng 4.12、syslog-ng Premium Edition 8.2 和 syslog-ng Store Box 7.8 中提供。 | [1]https://github.com/syslog-ng/syslog-ng/security/advisories/GHSA-qwf9-6222-m24m |
| 50 | CVE-2026-41252 | 9.8  | CRITICAL | xrdp是一个开源的RDP服务器。版本0.10.6及之前的版本在xrdp中存在一个缺失的边界检查，这在vnc-any模式下操作时允许基于堆的缓冲区溢出。该问题发生在处理来自VNC服务器的RFB协议颜色映射消息时，其中传入的颜色索引没有得到适当验证。恶意的VNC服务器可以通过发送带有超出范围值的精心构造的消息来利用此漏洞，导致堆上的越界写入。这种内存损坏可能导致拒绝服务（DoS）或潜在地允许在认证前远程代码执行（RCE）。此问题已在版本0.10.6.1中得到修复。 | [1]https://github.com/neutrinolabs/xrdp/releases/tag/v0.10.6.1<br>[2]https://github.com/neutrinolabs/xrdp/security/advisories/GHSA-w5vg-6qmv-j63j |
| 51 | CVE-2026-41521 | 8.2  | HIGH | xrdp是一个开源的RDP服务器。0.10.6及之前的版本在处理vnc-any连接模式下的屏幕更新消息时存在整数溢出漏洞。恶意的远程VNC服务器可以发送经过精心构造的图像尺寸，导致在计算内存缓冲区大小时发生整数溢出，从而导致分配的内存不足。随后使用原始过大的参数处理传入的图像数据会导致越界读取。未经身份验证的远程攻击者可以利用此漏洞从堆内存中泄露敏感信息或通过进程崩溃造成拒绝服务（DoS）。此问题已在版本0.10.6.1中得到修复。 | [1]https://github.com/neutrinolabs/xrdp/releases/tag/v0.10.6.1<br>[2]https://github.com/neutrinolabs/xrdp/security/advisories/GHSA-v8w6-pf78-9458 |
| 52 | CVE-2026-44178 | 8.8  | HIGH | xrdp 是一个开源的 RDP 服务器。版本 0.10.6 及之前的版本在虚拟通道转发机制中包含一个基于堆的缓冲区溢出漏洞。当从远程客户端向内部通道服务器转发数据时，xrdp 进程使用了一个固定大小的缓冲区，而没有对传入的数据负载进行充分的边界检查。经过身份验证的远程攻击者可以通过发送特制的虚拟通道消息来利用此漏洞，这些消息超出缓冲区容量，导致堆内存损坏。这可能导致拒绝服务或以 xrdp 进程权限执行任意代码。此问题已在版本 0.10.6.1 中修复。 | [1]https://github.com/neutrinolabs/xrdp/releases/tag/v0.10.6.1<br>[2]https://github.com/neutrinolabs/xrdp/security/advisories/GHSA-hh7r-2rmq-q4g4 |
| 53 | CVE-2026-46555 | 7.7  | HIGH | WhatsApp MCP 服务器是用于 WhatsApp 的模型上下文协议（MCP）服务器，使 Claude 能够读取和发送 WhatsApp 消息。在 0.2.1 版本之前，`whatsapp-bridge` HTTP API 在 `127.0.0.1:8080` 上监听，没有身份验证且没有 Host 标头验证，而 `/api/send` 端点接受一个绝对的 `media_path` 参数，但没有将其限制在一个安全目录内。这些问题结合起来，允许以与桥接器相同的用户身份运行的任何本地进程在未经授权的情况下从配对账户发送 WhatsApp 消息；相同的调用者可以读取该用户可访问的任意文件（例如 SSH 私钥、浏览器会话数据、源代码、配置文件），并将它们作为 WhatsApp 文档附件外泄；或者远程攻击者可以通过用户访问的网页上的 DNS 重新绑定触发相同的操作，因为没有执行 Host 标头验证。在 MCP 环境中，“本地调用者”不仅限于用户显式启动的进程——同级的 MCP 服务器、IDE 扩展以及在用户会话中运行的工具触发的流程都可以充当实际的调用者。此问题已在 whatsapp-mcp v0.2.1 及相应的 Docker 镜像/发布工件中修复。用户应立即升级。修复措施包括在桥接 HTTP API 上引入承载令牌身份验证（通过环境变量配置，所有请求都需要，并使用恒定时间比较进行验证）；主机标头允许列表验证以防止 DNS 重新绑定；将 `media_path` 限制在一个配置的目录内，并拒绝根目录外的绝对路径和路径遍历序列。这对桥接 API 的客户端来说是一个破坏性变更。对于无法立即升级的用户：当桥接器不活跃使用时，停止桥接器或阻止回环访问端口 8080；避免与不受信任的 MCP 服务器、浏览器扩展或其他不受信任的本地进程一起运行桥接器；在桥接器运行时避免浏览不受信任的网站（DNS 重新绑定缓解）；和/或在没有访问敏感文件权限的专用用户账户或沙箱/容器中运行桥接器。 | [1]https://github.com/verygoodplugins/whatsapp-mcp/blob/main/SECURITY.md<br>[2]https://github.com/verygoodplugins/whatsapp-mcp/releases/tag/v0.2.1<br>[3]https://github.com/verygoodplugins/whatsapp-mcp/security/advisories/GHSA-7jj9-4qqq-4xc4 |
| 54 | CVE-2026-46701 | 7.6  | HIGH | Network-AI是一个TypeScript/Node.js多代理编排器。在5.4.5版本之前，MCP SSE服务器默认使用一个空的密钥（`process.env[`NETWORK_AI_MCP_SECRET`] ??  在 `bin/mcp-server.ts:89`），这导致 `_isAuthorized` （`lib/mcp-transport-sse.ts:254`）对每个请求无条件返回`true`——不需要`Authorization`头。同时，`_handleRequest` 对每个响应设置 `Access-Control-Allow-Origin: *` （`lib/mcp-transport-sse.ts:272`），因此跨源浏览器获取可以不受限制地读取结果。一个未经认证的攻击者如果能够诱使用户访问恶意网页，则可以针对默认配置的本地服务器调用所有22个暴露的MCP工具——包括`config_set`、`agent_spawn`和`blackboard_write`。5.4.5版本修复了这个问题。 | [1]https://github.com/Jovancoding/Network-AI/commit/dc5048112283f3f4eb6c06dd2bf5aa93ef9339be<br>[2]https://github.com/Jovancoding/Network-AI/releases/tag/v5.4.5<br>[3]https://github.com/Jovancoding/Network-AI/security/advisories/GHSA-j3vx-cx2r-pvg8 |
| 55 | CVE-2026-54051 | 9.9  | CRITICAL | Network-AI 是一个 TypeScript/Node.js 多代理编排器。在 5.9.1 版本之前，代理沙箱通过允许列表（`SandboxPolicy.isCommandAllowed`）来限制 shell 命令的执行，THREAT_MODEL.md 称这是针对被攻陷代理（对手 3.2）的主要控制措施。允许列表使用 glob 匹配整个命令字符串，但 `ShellExecutor` 会将该字符串通过 `/bin/sh -c` 来运行。因此，任何通配符允许项，如 `git *`、`npm *` 或 `node *` 也会匹配 `git status; <anything>`，从而使范围限定的命令变为任意执行。此问题已在 v5.9.1 中修复。`ShellExecutor` 现在通过 `spawn(file, args, { shell: false })` 使用支持引号解析的 argv 来执行，因此不会调用 shell。`SandboxPolicy.isCommandAllowed` 和新的 `SandboxPolicy.tokenizeCommand` 会拒绝任何未加引号的 shell 元字符（`; &`）。 | $ ` ` ` ( ) < > { }` newline) or unterminated quote before the allowlist glob match; quoted metacharacters are preserved as literal argument data. Users should upgrade to `network-ai@5.9.1` or later. As defense in depth, avoid broad wildcard allowlist entries such as `node *` / `npm *` which are direct code execution by design. | [1]https://github.com/Jovancoding/Network-AI/commit/379f77656b578144e03415c5b134d8309a4b5792<br>[2]https://github.com/Jovancoding/Network-AI/security/advisories/GHSA-qw6v-5fcf-5666 |
| 56 | CVE-2026-54538 | 7.5  | HIGH | xrdp 是一个开源的 RDP 服务器。在 0.10.6 及之前的版本中，发现了一个问题，即软件在接收数据包时未能正确验证 RDP 协议控制头中的 totalLength 字段。未经身份验证的远程攻击者可以通过发送特制的数据包来利用此漏洞，迫使 xrdp 进程或线程进入无限的、CPU 密集型循环。由于内部指针未能前进，并且针对特定协议数据单元类型绕过了死锁预防机制，进程将无限制地消耗过多的 CPU 资源。这可能会导致 xrdp 服务不可用，并且如果建立了多个恶意连接，可能会导致系统范围的资源耗尽。此问题已在 0.10.6.1 版本中得到修复。 | [1]https://github.com/neutrinolabs/xrdp/releases/tag/v0.10.6.1<br>[2]https://github.com/neutrinolabs/xrdp/security/advisories/GHSA-9j3q-9mvw-qv7j |
| 57 | CVE-2026-58484 | 7.1  | HIGH | Network-AI 是一个 TypeScript/Node.js 多代理编排器。在 5.12.2 版本之前，`EnvironmentManager.listBackups()` 会读取每个备份的 `_manifest.json` 并信任该清单中的 `path` 字段。`EnvironmentManager.pruneBackups()` 随后会直接将信任的 `entry.path` 传递给 `rmSync(entry.path, { recursive: true, force: true })`。攻击者如果能够在 `data/<env>/.backups/<name>/_manifest.json` 中放置或修改清单，则可以导致 `network-ai env backup prune --env <env> --keep <n>` 或任何调用 `pruneBackups()` 的代码路径递归删除 Network-AI 进程用户可访问的任意路径。此问题已在 v5.12.2 版本中修复。`pruneBackups()` 不再将磁盘上的清单中的 `entry.path` 传递给 `rmSync`。删除路径是从格式验证后的 `entry.backupId` 重新计算的，并且通过 `dirname` 包含检查将删除限制在备份目录下的一个层级内。现在，被污染的清单（例如 `"path": "/"`）已经无效。 | [1]https://github.com/Jovancoding/Network-AI/commit/a59c13a1f0ce0e8a0779a90343eef92fac5ab4c3<br>[2]https://github.com/Jovancoding/Network-AI/releases/tag/v5.12.2<br>[3]https://github.com/Jovancoding/Network-AI/security/advisories/GHSA-2fmp-9rvw-hc96<br>[4]https://github.com/Jovancoding/Network-AI/security/advisories/GHSA-2fmp-9rvw-hc96 |
| 58 | CVE-2026-39878 | 9.3  | CRITICAL | Chamilo LMS 1.11.38 及更早版本在用户注册表单中包含一个存储型跨站脚本漏洞，该漏洞允许任何未经身份验证的攻击者在管理员的浏览器会话中执行任意 JavaScript，从而导致完全接管平台管理员账户。此问题已在 1.11.40 版本中得到修复。 | [1]https://github.com/chamilo/chamilo-lms/security/advisories/GHSA-gcjp-f7jm-rrrg |
| 59 | CVE-2026-48812 | 7.5  | HIGH | FreeScout 是一个使用 PHP 的 Laravel 框架构建的免费帮助台和共享收件箱。在 1.8.221 版本之前，FreeScout 的附件下载路由会跳过 `token_type` 设置为 `1`（即 `TOKEN_TYPE_LEGACY`）的任何附件的令牌认证。由于该路由未经认证且文件路径是确定性的，因此未经认证的远程攻击者可以下载由 FreeScout 较旧版本创建的任何附件，而无需持有有效的令牌或会话。1.8.221 版本包含了对此问题的修复。 | [1]https://github.com/freescout-help-desk/freescout/commit/215241ee2eb73eaa3b47e392599c7dc1b427dc7e<br>[2]https://github.com/freescout-help-desk/freescout/security/advisories/GHSA-wg74-ww4w-2qpc<br>[3]https://github.com/freescout-help-desk/freescout/security/advisories/GHSA-wg74-ww4w-2qpc |
| 60 | CVE-2026-55626 | 8.0  | HIGH | xrdp是一个开源的RDP服务器。在0.10.6及之前的版本中，当使用UNIX域套接字通过Xvnc后端初始化已认证用户会话时，Xvnc进程是以不足的认证机制启动的。本地已认证攻击者可以利用此漏洞绕过预期的会话隔离，使他们未经授权地查看或控制同一系统上其他用户的活动桌面会话。使用其他后端（如xorgxrdp或通过TCP套接字的Xvnc）的用户不受影响。此问题已在版本0.10.6.1中得到修复。 | [1]https://github.com/neutrinolabs/xrdp/releases/tag/v0.10.6.1<br>[2]https://github.com/neutrinolabs/xrdp/security/advisories/GHSA-m3xx-cpc4-982r |
| 61 | CVE-2026-64612 | 7.5  | HIGH | 在 libcupsfilters 和 cups-filters 中发现了一个缺陷。PNG 图像读取函数创建了一个 libpng 读取器，但没有安装错误恢复处理程序，这会导致 CUPS 图像过滤进程在处理格式错误的 PNG 文件时中止。未经身份验证的攻击者可以通过提交特制的 PNG 打印作业来利用这一点，从而导致正在进行的打印作业服务中断。 | [1]https://access.redhat.com/security/cve/CVE-2026-64612<br>[2]https://bugzilla.redhat.com/show_bug.cgi?id=2502801<br>[3]https://github.com/OpenPrinting/libcupsfilters/security/advisories/GHSA-7mxj-cfq5-84ch |
| 62 | CVE-2026-12341 | 8.8  | HIGH | 此漏洞影响所有版本的IdentityIQ，由于对OAuth承载令牌的验证不当，允许未经身份验证的攻击者未经授权访问受保护的API和数据。 | [1]https://www.sailpoint.com/security-advisories/ |
| 63 | CVE-2026-48389 | 7.8  | HIGH | DNG SDK 版本 1.7.1 2536 及更早版本受到一个基于栈的缓冲区溢出漏洞的影响，这可能导致在当前用户上下文中执行任意代码。利用此问题需要用户交互，即受害者必须打开一个恶意文件。 | [1]https://helpx.adobe.com/security/products/dng-sdk/apsb26-67.html |
| 64 | CVE-2026-63107 | 7.7  | HIGH | LimeSurvey 6.17.10及以下版本和7.0.4版本在REST API调查模板端点中包含一个服务器端请求伪造漏洞，该漏洞允许经过身份验证的用户通过提供被篡改的Host头来使服务器发出任意HTTP请求。攻击者可以利用getTemplateData()函数中未经处理的HTTP Host头来访问内部网络服务、云元数据端点，并从实例元数据服务中提取敏感凭证，如IAM令牌。 | [1]https://github.com/geo-chen/oss/blob/main/limesurvey.md<br>[2]https://www.vulncheck.com/advisories/limesurvey-ssrf-via-rest-api-survey-template-host-header |
| 65 | CVE-2026-63108 | 8.8  | HIGH | Roo Code 在 3.54.0 版本之前包含一个命令注入漏洞，该漏洞存在于自动批准执行功能中。攻击者可以通过在参数扩展默认值内嵌套命令替换来绕过允许列表/拒绝列表的强制执行。`parse-command.ts` 中的命令解析器在提取命令替换之前将参数扩展替换为不透明的占位符，导致 `containsDangerousSubstitution` 守护机制未能检测到嵌套的有效载荷。这些有效载荷随后根据外部允许的命令前缀被自动批准，并通过 `execa` 由 shell 执行，从而实现任意命令执行。 | [1]https://github.com/geo-chen/oss/blob/main/Roo-Code.md<br>[2]https://www.vulncheck.com/advisories/roo-code-command-injection-via-parameter-expansion-parsing |
| 66 | CVE-2026-63731 | 7.7  | HIGH | HyperDX 在 2.31.0 版本之前包含一个服务器端请求伪造漏洞，该漏洞允许经过身份验证的团队成员通过向 ClickHouse 代理测试端点提供一个由调用者控制的主机参数来将服务器重定向到任意内部目的地，而无需进行 URL 验证或白名单强制执行。攻击者可以利用该端点反射的错误响应来披露内部服务响应体，从而能够访问内部 API、容器服务和云提供商元数据端点。 | [1]https://github.com/hyperdxio/hyperdx/commit/1705b37ac68acc222cd038327ed79e167e256a1b<br>[2]https://github.com/hyperdxio/hyperdx/issues/2588<br>[3]https://github.com/hyperdxio/hyperdx/pull/2593<br>[4]https://github.com/hyperdxio/hyperdx/releases/tag/%40hyperdx%2Fapp%402.31.0<br>[5]https://www.vulncheck.com/advisories/hyperdx-ssrf-via-clickhouse-proxy-test-endpoint |
| 67 | CVE-2026-63769 | 7.7  | HIGH | Huginn 在 2022年8月18日之前包含一个服务器端请求伪造漏洞，该漏洞存在于ScenarioImport的fetch_url方法中，允许经过身份验证的用户通过提交精心构造的URL来发起任意HTTP请求。攻击者可以探测内部网络服务，通过错误签名枚举端口，并访问云元数据端点以获取敏感凭证。 | [1]https://github.com/huginn/huginn/issues/3679<br>[2]https://github.com/huginn/huginn/pull/3684<br>[3]https://www.vulncheck.com/advisories/huginn-ssrf-via-scenarioimport-fetch-url-method |
| 68 | CVE-2026-63770 | 7.5  | HIGH | Glance 0.8.5 存在一个 IP 地址欺骗漏洞，在启用服务器代理选项时，该漏洞允许未经身份验证的攻击者通过在 X-Forwarded-For 请求头中提供任意值来绕过暴力破解锁定保护。攻击者可以操纵 X-Forwarded-For 头中最左边的值，使每次登录尝试看起来都来自不同的 IP 地址，从而阻止每个 IP 的失败登录计数器达到锁定阈值，并允许对认证端点进行无限的凭据猜测。 | [1]https://github.com/glanceapp/glance/issues/1031<br>[2]https://github.com/glanceapp/glance/pull/1033<br>[3]https://www.vulncheck.com/advisories/glance-ip-spoofing-authentication-brute-force-protection-bypass |
| 69 | CVE-2026-63771 | 7.1  | HIGH | Adminer 在 5.4.3 之前的版本中包含一个 cookie 注入漏洞，该漏洞允许攻击者通过在未经过滤的 X-Forwarded-Prefix HTTP 头中注入任意值来操纵 cookie 属性，这些值被用于 Set-Cookie 路径属性中。攻击者可以利用配置错误的反向代理来降低 SameSite 保护级别，并启用跨源身份验证请求，从而绕过 cookie 安全控制。 | [1]https://github.com/vrana/adminer/issues/1298<br>[2]https://github.com/vrana/adminer/releases#release-v5.4.3<br>[3]https://github.com/vrana/adminer/security/advisories/GHSA-c533-9qwm-8w5h<br>[4]https://www.vulncheck.com/advisories/adminer-cookie-injection-via-x-forwarded-prefix-header |
| 70 | CVE-2026-64619 | 7.5  | HIGH | FileCodeBox 在 2.4 版本之前存在一个速率限制绕过漏洞，在 IPRateLimit 类中，该漏洞允许未经身份验证的攻击者通过提供攻击者控制的 X-Real-IP 和 X-Forwarded-For 头来绕过请求节流，而无需验证可信的反向代理来源。攻击者可以在每次请求时提供唯一的伪造 IP 值，以枚举所有可能的共享代码，并在未经身份验证的情况下检索其他用户的文件。 | [1]https://github.com/vastsa/FileCodeBox/commit/1b6d8e7277d3cfa34dc7a85803731d927b2147da<br>[2]https://github.com/vastsa/FileCodeBox/issues/479<br>[3]https://github.com/vastsa/FileCodeBox/releases/tag/V2.4<br>[4]https://www.vulncheck.com/advisories/filecodebox-anti-bruteforce-rate-limit-bypass-via-spoofed-headers |
| 71 | CVE-2026-44231 | 9.1  | CRITICAL | RT 是一个开源的企业级问题和工单跟踪系统。在 5.0.10、6.0.0 及以上版本之前（但不包括 6.0.3），REST 2.0 API 中存在信息泄露和权限提升漏洞。具有特权（非管理员）的用户可以获取其他用户（包括具有管理员权限的用户）的身份验证凭据，并使用这些凭据通过 RT 的 feed 端点读取数据。暴露凭据的同一请求还会轮换这些凭据，从而使实例中之前分发的 feed URL 失效。此问题已在 5.0.10 和 6.0.3 版本中得到修复。 | [1]https://github.com/bestpractical/rt/releases/tag/rt-6.0.3<br>[2]https://github.com/bestpractical/rt/security/advisories/GHSA-7rx2-x357-wv74 |
| 72 | CVE-2026-53591 | 8.6  | HIGH | FreeScout 是一个使用 PHP 的 Laravel 框架构建的免费帮助台和共享收件箱。在 1.8.223 版本之前，未经身份验证的攻击者可以通过向帮助台的公共地址发送一封带有精心构造的 `In-Reply-To` 头的电子邮件，将消息注入到任何现有的支持对话中。无需提供凭据、令牌或之前的访问权限。注入的消息会在代理界面中显示为合法的客户回复，对话会自动重新打开，并且 `last_reply_from` 字段会被设置为攻击者的身份。1.8.223 版本包含了对此问题的修复。 | [1]https://github.com/freescout-help-desk/freescout/security/advisories/GHSA-8vm3-wwq4-ggfx<br>[2]https://github.com/freescout-help-desk/freescout/security/advisories/GHSA-8vm3-wwq4-ggfx |
| 73 | CVE-2026-53593 | 8.8  | HIGH | FreeScout 是一个使用 PHP 的 Laravel 框架构建的免费帮助台和共享收件箱。在 1.8.224 版本之前，用于中和危险文件上传的黑名单 (`Helper::$restricted_extensions`) 是不完整的：它没有涵盖 `.pht` 扩展名。经过身份验证的上传端点 `POST /uploads/upload` (`SecureController@upload`) 会将文件以其原始扩展名存储在网络可访问的目录 `storage/app/public/uploads/` 中（通过 `/storage/uploads/` 提供服务）。在标准的 Apache + `libapache2-mod-php` 部署中，默认处理器 `<FilesMatch ".+\.ph(ar |p[3457]?|t|tml)$">` executes `.pht`, so **any authenticated agent can upload a `.pht` web shell and run arbitrary commands as the web-server user** (`www-data`). This is a direct bypass of the fix for CVE-2025-48471, which added `phtml`/`phar` but not `pht` (nor `phtm`, `phps`). Version 1.8.224 contains an updated fix. | [1]https://github.com/freescout-help-desk/freescout/security/advisories/GHSA-27vp-fpg8-j8wv |
| 74 | CVE-2026-63766 | 9.8  | CRITICAL | GPT-SoVITS 截至 20250606v2pro 版本在 webui.py 中存在一个操作系统命令注入漏洞，其中 ASR、slice、denoise 和 uvr5 函数将未经处理的 Gradio 文本框值直接插入到使用 shell=True 执行的 shell 命令中。攻击者可以通过路径参数注入 shell 元字符，从而在未经身份验证的情况下以服务器进程用户的权限执行任意操作系统命令。 | [1]https://github.com/RVC-Boss/GPT-SoVITS/issues/2793<br>[2]https://www.vulncheck.com/advisories/gpt-sovits-20250606v2pro-os-command-injection-via-webui-py |
| 75 | CVE-2026-63767 | 9.8  | CRITICAL | ktransformers 0.6.3及以下版本，在提交def0f93中已修复，包含一个未经身份验证的pickle反序列化漏洞，该漏洞允许远程攻击者通过向绑定到所有接口的SchedulerServer ZMQ ROUTER套接字发送精心构造的pickle载荷来执行任意命令。攻击者可以利用嵌入在精心构造的pickle载荷中的恶意__reduce__方法，以服务器进程的身份执行任意shell命令。 | [1]https://github.com/kvcache-ai/ktransformers/commit/def0f9313d6e063b5c5ccdfa1f6707f7a40dfdca<br>[2]https://github.com/kvcache-ai/ktransformers/issues/2087<br>[3]https://github.com/kvcache-ai/ktransformers/pull/2091<br>[4]https://www.vulncheck.com/advisories/ktransformers-unauthenticated-pickle-deserialization-rce-via-zmq |
| 76 | CVE-2026-44508 | 8.1  | HIGH | Rsync 是一种文件复制工具，它使用增量传输算法来同步远程和本地文件。在 3.4.3 版本之前，接收方的压缩令牌解码器会在不检查溢出的情况下累积一个 32 位有符号计数器。恶意发送者可以触发溢出，通过精心操控可以导致进程中存储的数据被提取，使攻击者能够访问环境变量、密码以及来自堆、栈和库的内存指针。这些指针和数据的泄露会显著降低 ASLR 的有效性，并有助于进一步的利用。此问题已在 3.4.3 版本中得到修复。 | [1]https://github.com/RsyncProject/rsync/security/advisories/GHSA-g37v-g3gj-pmwq |
| 77 | CVE-2026-47129 | 8.1  | HIGH | NextCRM是一款开源的客户关系管理（CRM）软件。在0.12.0版本之前，NextCRM的`activateUser`和`deactivateUser` Next.js服务器操作中存在一个访问控制缺陷（BAC）。应用程序未能验证请求用户是否具有`admin`角色。因此，任何经过身份验证的用户（即使是那些具有最低`member`或`viewer`角色的用户）都可以任意激活或停用系统中的任何用户账户，包括主管理员。0.12.0版本修复了这个问题。 | [1]https://github.com/pdovhomilja/nextcrm-app/releases/tag/v0.12.0<br>[2]https://github.com/pdovhomilja/nextcrm-app/security/advisories/GHSA-gm7p-f88p-vhfr |
| 78 | CVE-2026-47130 | 7.1  | HIGH | NextCRM是一款开源的客户关系管理（CRM）软件。在0.12.0版本之前，该软件在CRM联系人和目标更新端点上存在一个对象级别授权破坏（BOLA/IDOR）漏洞。应用程序未能验证经过身份验证的用户是否拥有被修改的具体资源的所有权。这使得任何经过身份验证的用户（即使是具有标准`成员`角色的用户）都能够任意修改属于其他用户或组织的敏感CRM联系人和目标（跨租户数据篡改）。0.12.0版本修复了这个问题。 | [1]https://github.com/pdovhomilja/nextcrm-app/security/advisories/GHSA-mg5f-m89f-4gmc |
| 79 | CVE-2026-47198 | 8.5  | HIGH | Paymenter 是一个免费且开源的网络商店解决方案，用于管理托管服务。在 1.5.1 版本之前，结账组件未能正确过滤 URL 可写属性，允许已认证用户向服务器配置参数中注入任意键值对。由于捆绑的服务器扩展优先使用用户提供的属性而不是管理员定义的配置，普通用户可以在结账时无需特殊权限就覆盖托管计划和资源限制。结账 Livewire 组件的 $checkoutConfig 属性通过 URL 查询参数暴露，仅验证扩展配置方法中明确定义的键，允许任何未定义的注入键完全绕过验证。这些未经处理的键随后由购物车组件直接存储在数据库中，并在供应过程中传递给服务器扩展，使用户注入的数据能够覆盖管理员设置的意图。根据激活的扩展，这会导致未经授权的核心资源限制（如 CPU、RAM、存储或套餐级别）被覆盖。利用此漏洞不需要管理权限。此问题已在 1.5.1 版本中修复。 | [1]https://github.com/Paymenter/Paymenter/security/advisories/GHSA-5q4q-834j-g8g4 |
| 80 | CVE-2026-53595 | 9.4  | CRITICAL | FreeScout 是一个使用 PHP 的 Laravel 框架构建的免费帮助台和共享收件箱。在 1.8.224 版本之前，公共端点 `POST /user-setup/{hash}/{invite_sent_at}` (`OpenController@userSetupSave`) 仅通过其 `invite_hash` 列来选择目标账户，然后覆盖该账户的电子邮件和密码并以该账户身份登录。不需要任何身份验证、cookie 或之前的会话。用户激活后，FreeScout 将 `invite_hash` 设置为空字符串。在 MySQL 和 MariaDB 中，`VARCHAR` 等式忽略尾随空格，因此单个 URL 编码的空格（`%20`）与存储的空字符串匹配，并选择最低 ID 的已激活用户。过期保护通过目标的密码哈希解密 `invite_sent_at`，但当解密失败时，`Helper::decrypt` 返回其原始输入不变。因此，纯文本数值（如 `9999999999`）可以在没有任何密钥的情况下通过生存时间检查。结果是，匿名攻击者可以设置最低 ID 的已激活 FreeScout 账户（支持代理，或通过邀请添加的管理员）的电子邮件和密码，并以该账户身份进行认证。1.8.224 版本包含对此问题的修复。 | [1]https://github.com/freescout-help-desk/freescout/security/advisories/GHSA-jqj5-r72v-v29g |
| 81 | CVE-2026-56452 | 7.5  | HIGH | 路径遍历存在于 Apache MINA SSHD 的 sshd-scp 组件中。Apache MINA SSHD 是一个用于客户端和服务器端 SSH 的 Java 库。在通过 SCP 接收文件或目录的实现中，没有验证 SCP "C" 或 "D" 命令中的文件名。恶意发送者可以发送包含路径的文件名，从而导致文件被写入攻击者控制的位置。该问题仅影响：<br>* 使用不再支持的 Apache MINA SSHD 版本 < 2.0.0 并使用 SCP 功能接收文件的应用程序，<br>* 或使用 Apache MINA SSHD >= 2.0.0 中的 sshd-scp 接收文件的应用程序。<br>使用 Apache MINA SSHD >= 2.0.0 但不使用 sshd-scp 的应用程序不受影响。<br>该问题已在 Apache MINA 2.19.0 和 3.0.0-M5 中修复。建议受影响的应用程序升级到这些版本。 | [1]https://lists.apache.org/thread/xgoqvmksmd94fsqnzqjdtfjxf35os9no<br>[2]http://www.openwall.com/lists/oss-security/2026/07/20/15 |
| 82 | CVE-2026-56623 | 7.1  | HIGH | 在Windows上使用Apache MINA SSHD组件sshd-git时存在路径遍历漏洞。Apache MINA SSHD是一个用于客户端和服务器端SSH的Java库。如果使用Apache MINA SSHD组件sshd-git实现的git服务器运行在Windows上，可能会允许经过身份验证的远程用户访问配置的服务器端根目录之外的git仓库。针对CVE-2026-48827在Apache MINA SSHD 2.18.0和3.0.0-M4中应用的路径验证对于运行在Windows上的服务器部分无效。如果应用程序使用org.apache.sshd:sshd-git实现git服务器并运行在Windows上，则会受到影响。不使用sshd-git或不在Windows上运行的应用程序不会受到影响。建议用户将受影响的应用程序升级到Apache MINA SSHD 2.19.0，该版本修复了此问题。此问题也存在于新即将发布的主要版本3.0.0的预发布里程碑3.0.0-M1至3.0.0-M4中。同样，只有使用sshd-git并且运行在Windows上的应用程序才会受到影响。请将受影响的应用程序升级到3.0.0-M5。 | [1]https://lists.apache.org/thread/bhw26snzgvk0mtqqp5dcyjvczp4kcqky |
| 83 | CVE-2026-56624 | 7.3  | HIGH | Apache MINA SSHD（服务器端）中的证书验证不当。Apache MINA SSHD 是一个用于客户端和服务器端 SSH 的 Java 库。在 Apache MINA SSHD 服务器中，用户认证期间的 OpenSSH 用户证书验证没有检查证书中可能嵌入的不受支持的 force-command 或 verify-required 选项，也没有验证这些选项。因此，用户可能使用包含 force-command 选项的证书进行身份验证，但仍能够执行其他命令。具体哪些命令对用户可用取决于服务器的实现。<br>此问题已在 Apache MINA SSHD 2.19.0 和 3.0.0-M5 中修复。建议应用程序升级到这些版本。<br>修复措施拒绝包含这些选项的 OpenSSH 用户证书，因为 Apache MINA SSHD 既不实现 force-command 也不实现 sk-*-cert-v01@openssh.com 用户证书（后者是唯一使 verify-required 有意义的证书类型）。 | [1]https://lists.apache.org/thread/o4c2jml522j3z80gbryqzc2f1253ltp6<br>[2]http://www.openwall.com/lists/oss-security/2026/07/20/17 |
| 84 | CVE-2026-16324 | 7.3  | HIGH | 在美特软件 MetaCRM 6.4.0 Beta06 及以下版本中发现了一个漏洞。受影响的元素是文件 /business/qnaire/upload.jsp 中的一个未知函数。对参数 File 的这种操作会导致不受限制的文件上传。攻击可以远程发起。该漏洞利用方法已经公开，并可能被使用。供应商在早期就已被告知此披露，但未作出任何回应。 | [1]https://ucn9h68n9289.feishu.cn/docx/RTF0d4zxooIMA1xF9bGcnQNpnfg?from=from_copylink<br>[2]https://vuldb.com/cve/CVE-2026-16324<br>[3]https://vuldb.com/submit/858454<br>[4]https://vuldb.com/vuln/380685<br>[5]https://vuldb.com/vuln/380685/cti |
| 85 | CVE-2026-47255 | 8.2  | HIGH | AgenticMail 为 AI 代理提供真实的电子邮件地址和电话号码。@agenticmail/api 在 0.9.32 版本之前和 @agenticmail/core 在 0.9.10 版本之前存在与验证和非活动代理小时过滤绑定相关的弱点；存储 SQL 标识符验证；基于元数据的所有权检查用于原始存储 SQL；通过原始 SQL 阻止直接访问存储元数据；故障关闭的出站工作器密钥处理；在构造命令前对 SMTP 信封/标头控制字符进行验证；以及将 TLS 证书验证作为 MailSender 的默认设置，并明确选择退出本地开发。@agenticmail/api 在 0.9.32 版本之前和 @agenticmail/core 在 0.9.10 版本之前的版本已修复。 | [1]https://github.com/agenticmail/agenticmail/blob/7b9b05d973676e9f3d097c08b8e649f59bfc15d0/CHANGELOG.md?plain=1#L1842<br>[2]https://github.com/agenticmail/agenticmail/blob/7b9b05d973676e9f3d097c08b8e649f59bfc15d0/packages/core/src/mail/sender.ts#L33<br>[3]https://github.com/agenticmail/agenticmail/commit/1408de543fa3577d8c2d4fdb289c75fe6faafac7<br>[4]https://github.com/agenticmail/agenticmail/commit/234b811e426a0743170f3b10bc43419d64330155<br>[5]https://github.com/agenticmail/agenticmail/commit/6c70c8254c906f823392d7f5ccee88a5481e7731<br>[6]https://github.com/agenticmail/agenticmail/commit/8cb053f2307dd77b7736ffa0d7df04b0ccc3272d<br>[7]https://github.com/agenticmail/agenticmail/security/advisories/GHSA-wjjv-3mj2-39hf |
| 86 | CVE-2026-55544 | 7.6  | HIGH | NextCRM是一款开源的客户关系管理（CRM）软件。在0.12.1版本中，MCP活动工具通过用户生成的Bearer API令牌（`nxtc__...`）在网络上暴露了活动的读取和写入操作。该应用程序有一个授权模型，限制普通用户只能访问他们自己创建的活动，但多个MCP活动处理器忽略了已认证用户的ID，仅通过对象ID来查询或修改活动。因此，一个具有较低权限且持有有效MCP API令牌的认证用户可以枚举所有活动、读取活动详情、更新或删除其他用户拥有的活动、修改活动模板和步骤，并可能触发或暂停活动投递。0.12.2版本修复了这个问题。 | [1]https://github.com/pdovhomilja/nextcrm-app/security/advisories/GHSA-c9vg-c532-ppqx |
| 87 | CVE-2026-55550 | 7.1  | HIGH | NextCRM是一款开源的客户关系管理（CRM）软件。CRM产品目录是整个组织范围内的业务对象。通常的应用服务器操作限制了只有`manager`和`admin`角色才能创建、更新和删除产品。然而，在0.12.1版本中，MCP产品工具通过`/api/mcp/mcp`使用用户生成的Bearer令牌暴露了相同的写操作，并且没有执行角色检查。任何经过身份验证的低权限用户，只要能够生成MCP API令牌，就可以在共享的CRM产品目录中创建、修改、归档或软删除产品。0.12.3版本包含了对此问题的修复。 | [1]https://github.com/pdovhomilja/nextcrm-app/security/advisories/GHSA-wv63-cq38-qg58<br>[2]https://github.com/pdovhomilja/nextcrm-app/security/advisories/GHSA-wv63-cq38-qg58 |
| 88 | CVE-2026-64624 | 7.8  | HIGH | FreeRDP 在 3.28.0 之前的版本中，将 RDP 文件中以正斜杠开头的行视为原始命令行选项，从而使整个 CLI 解析器表面暴露给不受信任的文件。攻击者可以制作带有 /rdp2tcp、/cert:ignore 或 /drive 选项的恶意 RDP 文件来执行任意命令、绕过证书验证或在没有用户交互的情况下暴露本地文件系统。 | [1]https://github.com/FreeRDP/FreeRDP/security/advisories/GHSA-rq8f-9xjh-pr3m<br>[2]https://www.vulncheck.com/advisories/freerdp-rdp-file-parser-remote-code-execution-via-cli-options |
| 89 | CVE-2026-64625 | 9.8  | CRITICAL | AVideo 在 29.0 版本之前对 CVE-2026-45578 的修复不完全，其中 execAsync() 将转义的命令重新包装在双引号内的 sh -c 中，允许通过 $() 和反引号进行命令替换。尽管有 escapeshellarg() 保护，攻击者仍可以通过 Live 插件的 on_publish.php 端点注入任意操作系统命令。 | [1]https://github.com/WWBN/AVideo/security/advisories/GHSA-rc5x-vh5v-473f<br>[2]https://www.vulncheck.com/advisories/avideo-before-os-command-injection-via-execasync |